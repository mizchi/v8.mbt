# oden hello world

最小の `oden run` 向けサンプルです。`oden` command が install 済みで、PATH に入っている前提です。

```bash
cd oden/examples/helloworld
oden run
```

`oden run` は guest project 側の `src/main` を既定 entry として使います。

`just` があれば次でも動きます。

```bash
cd oden/examples/helloworld
just run
```
